# 安全说明

## 这个仓库能保证什么

DSH Plugins 只整理公开信息并提供风险提示。我们会尽量核对仓库、安装命令、版本和作者声明，但**不会执行完整代码审计、供应链审计或恶意行为检测**。

## 安装第三方插件前

- 在没有生产密钥和敏感客户数据的环境先试装；
- 优先固定 Release、版本或 Commit；
- 阅读 `package.json`、Bundle Patch、安装脚本和新增权限；
- 特别关注 `curl | shell`、`prepare/postinstall`、SSH、远程访问、浏览器注入、凭证复用和外部 API；
- 一次只装一个插件，保留 Profile 配置与变更记录；
- 用系统级最小权限、容器或隔离账号降低影响面。

## 报告目录中的安全问题

若本仓库的描述、链接或安装命令会直接误导用户，请提交私密 Security Advisory 或 Issue，并包含：

- 受影响条目；
- 具体错误与可验证来源；
- 可能影响；
- 建议修正。

插件自身漏洞请优先报告给原项目维护者。除非存在迫在眉睫的用户风险，请给维护者合理修复时间后再公开细节。

## 凭证

不要在 Issue、PR、截图或示例配置中提交 API Key、Token、Webhook、Cookie、SSH 私钥或其他敏感信息。

